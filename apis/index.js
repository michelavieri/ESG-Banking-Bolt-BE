const express               = require("express");
const app                   = express();

// Login Route
app.post("/login", async(req, res) => {

})

// Logout Route
app.post("/logout", async(req, res) => {

})

module.exports = app;